# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# Compiled binaries (rebuild via install.sh / install.ps1)
stream-reuse
stream-reuse.exe
brute-extend
brute-extend.exe
direct-decrypt
direct-decrypt.exe
frank
_stream-reuse/stream-reuse
_stream-reuse/stream-reuse.exe
_stream-reuse/direct_decrypt_v2
_stream-reuse/direct_decrypt_v2.exe
_stream-reuse/brute_extend_v2
_stream-reuse/brute_extend_v2.exe
_stream-reuse/frank

# Windows build artifacts
*.exe
*.dll
*.pdb

# Static libraries (vendored, rebuildable)
*.a

# Embedded git repos
_stream-reuse/.git/

# Recovery artifacts
.stream-reuse/
*.log
manifest.csv
verification_report.json
.recovery_state.json
.scratch/
decrypted/
decrypted

# Test data (generated at runtime)
lockbit-rescue-v2/test_data/synthetic_lockbit/

# IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
